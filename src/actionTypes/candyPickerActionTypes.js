const actionPrefix = 'candyPicker';

const UserActionTypes = {
  POST_CANDY_PICKER_COMMAND: `${actionPrefix}/POST_CANDY_PICKER_COMMAND`
};

const InternalActionTypes = {
 
};

export default {
  UserActionTypes,
  InternalActionTypes
};
